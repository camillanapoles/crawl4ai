<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — Crawl4AI Android
  No cleartext traffic to production; allow localhost for development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system CAs for all production traffic -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext only to the Android emulator host and localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
