<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Crawl4AI uses a fully dynamic Material 3 theme; this style is the
         window-level base theme only. The actual colour scheme is set in
         Compose via MaterialTheme. -->
    <style name="Theme.Crawl4AI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Edge-to-edge: let the system status/nav bars be transparent.
             Actual padding is applied in Compose via WindowInsets. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Splash screen theme — replaces the white flash on cold start -->
    <style name="Theme.Crawl4AI.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Crawl4AI</item>
    </style>
</resources>
